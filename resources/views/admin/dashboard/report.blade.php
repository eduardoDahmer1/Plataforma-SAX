<!doctype html><html lang="{{ str_replace('_', '-', app()->getLocale()) }}"><head><meta charset="utf-8"><link rel="stylesheet" href="{{ public_path('css/admin-report.css') }}"></head><body><div class="head"><h1>{{ __('messages.report_general_title') }}</h1><p>{{ $report['period'] }} · {{ $report['start']->format('d/m/Y') }} {{ __('messages.date_range_to') }} {{ $report['end']->format('d/m/Y') }}</p></div>
<table class="grid"><tr><td class="metric"><div class="label">{{ __('messages.report_paid_sales') }}</div><div class="value">{{ $report['paid_orders'] }}</div></td><td class="metric"><div class="label">{{ __('messages.report_sold_value_base') }}</div><div class="value">US$ {{ number_format($report['sales_total'],2,',','.') }}</div></td><td class="metric"><div class="label">{{ __('messages.report_orders_created') }}</div><div class="value">{{ $report['orders'] }}</div></td></tr><tr><td class="metric"><div class="label">{{ __('messages.report_new_customers') }}</div><div class="value">{{ $report['new_customers'] }}</div></td><td class="metric"><div class="label">{{ __('messages.report_visitors') }}</div><div class="value">{{ $report['visitors'] }}</div></td><td class="metric"><div class="label">{{ __('messages.report_clicks') }}</div><div class="value">{{ $report['clicks'] }}</div></td></tr><tr><td class="metric"><div class="label">{{ __('messages.report_page_views') }}</div><div class="value">{{ $report['views'] }}</div></td><td class="metric"><div class="label">{{ __('messages.report_abandoned_carts') }}</div><div class="value">{{ $report['abandoned_carts'] }}</div></td><td></td></tr></table>
<h2>{{ __('messages.report_orders_by_payment') }}</h2><table class="data"><thead><tr><th>{{ __('messages.report_method') }}</th><th>{{ __('messages.report_orders') }}</th></tr></thead><tbody>@forelse($report['payment_methods'] as $method => $total)<tr><td>{{ ['bancard_v2'=>'Bancard V2','rendix_pix'=>'Pix Rendix','deposito'=>__('messages.payment_deposit'),'whatsapp'=>'WhatsApp'][$method] ?? ucfirst($method) }}</td><td>{{ $total }}</td></tr>@empty<tr><td colspan="2">{{ __('messages.report_no_orders_period') }}</td></tr>@endforelse</tbody></table><div class="foot">{{ __('messages.report_generated_at', ['date' => now()->format('d/m/Y H:i')]) }} {{ __('messages.report_disclaimer') }}</div></body></html>
